Nice, now we do **FinaFinance / FinaBridge Admin Panel** for Replit.
Here’s a **ready-to-paste Replit dev prompt** 👇

---

````txt
You are a senior frontend engineer.
Build a **FinaFinance (FinaBridge) Admin Panel** UI for the Finatrades back-office.

Context:
FinaFinance is the **trade finance wallet**:
- Importers transfer gold from **FinaPay → FinaFinance Trade Wallet**.
- That gold is locked as: “Reserved – Trade Finance Settlement Balance” in FinaVault.
- Exporter ships goods and uploads trade documents.
- Importer + Finatrades Compliance review documents.
- After final approval, **locked gold is released** from importer to exporter.
- All steps must be **compliant, auditable, and multi-party**.

This panel is for internal admins:
- Trade Finance Desk
- Operations
- Compliance / Risk

Admins should be able to:
- See all trade cases and their status.
- Inspect settlement locks (grams + USD).
- Review docs and approvals.
- Trigger release of funds (conceptual).
- View AML / compliance flags.
- See full audit log.

No backend yet → use **mock data + local state** only.

Tech:
- React (Vite + React or Next.js)
- TypeScript
- TailwindCSS
- Optional: framer-motion for subtle animations

Theme:
- Dark, premium, gold-backed:
  - Background: gradient #000000 → #0D0515 → #1A0A2E
  - Accent: #D1A954 (gold)
  - Cards: glassy (bg-white/5, border-white/10, backdrop-blur, rounded-2xl, shadow)
- Font: Inter

────────────────────────
ROUTE & LAYOUT
────────────────────────

Create component `/admin/finabridge` or `<FinaFinanceAdminPanel />`.

Layout:
- Left sidebar navigation.
- Right main content with:
  - Header
  - KPI summary row
  - Tabs (Cases / Documents / Locked Funds / Compliance / Audit)

Sidebar menu:
- Overview
- Trade Cases
- Locked Funds
- Compliance & Risk
- Audit Log
- Settings (stub)

Header:
- Title: “FinaBridge – Trade Finance Admin”
- Subtitle: “Manage gold-backed trade cases, documents, and settlements.”

────────────────────────
TYPES & MOCK STATE
────────────────────────

Use TypeScript to model trade finance data.

```ts
type TradeCaseStatus =
  | "Draft"
  | "Funding Pending"
  | "Funded – Docs Pending"
  | "Under Review"
  | "Approved – Ready to Release"
  | "Released"
  | "Closed"
  | "Rejected"
  | "On Hold";

type Role = "Importer" | "Exporter";

type KycStatus = "Not Started" | "In Progress" | "Approved" | "Rejected";

type RiskLevel = "Low" | "Medium" | "High" | "Critical";

type TradeParty = {
  id: string;
  name: string;
  role: Role;                // Importer / Exporter (per case)
  country: string;
  kycStatus: KycStatus;
  riskLevel: RiskLevel;
};

type LockStatus = "Not Locked" | "Lock Pending" | "Locked" | "Released" | "Partially Released";

type TradeCase = {
  id: string;
  reference: string;         // TF-2025-0007
  importer: TradeParty;
  exporter: TradeParty;
  contractNumber: string;
  commodityDescription: string;
  valueUsd: number;
  valueGoldGrams: number;
  lockedGoldGrams: number;   // in FinaFinance / FinaVault
  lockStatus: LockStatus;
  status: TradeCaseStatus;

  paymentTerms: string;      // e.g. "90 days after BL"
  deliveryTerms: string;     // FOB / CIF / EXW etc.
  shipmentMethod: string;    // Sea / Air / Land
  expectedDeliveryDate: string;

  createdAt: string;
  updatedAt: string;
  amlFlags: string[];        // e.g. ["High-risk jurisdiction"]
  jurisdictionRisk: RiskLevel;
};

type DocumentType =
  | "Invoice"
  | "Bill of Lading"
  | "Packing List"
  | "Certificate of Origin"
  | "Insurance Certificate"
  | "Inspection Report"
  | "Other";

type DocumentStatus =
  | "Missing"
  | "Uploaded"
  | "Under Review"
  | "Approved"
  | "Rejected";

type TradeDocument = {
  id: string;
  caseId: string;
  type: DocumentType;
  fileName: string;
  status: DocumentStatus;
  uploadedBy: string;        // importer/exporter user
  uploadedAt: string;
  remarks?: string;
};

type ApprovalStepStatus = "Pending" | "In Review" | "Approved" | "Rejected";

type ApprovalStep = {
  id: string;
  caseId: string;
  name: string;              // "Importer Verification", "Compliance Review"
  role: string;              // "Importer", "Compliance", "Ops"
  status: ApprovalStepStatus;
  approverName?: string;
  decisionAt?: string;
  notes?: string;
};

type AuditActionType =
  | "CaseCreated"
  | "CaseUpdated"
  | "FundingLocked"
  | "DocumentUploaded"
  | "DocumentReviewed"
  | "ApprovalUpdated"
  | "FundsReleased"
  | "CaseClosed"
  | "StatusChanged";

type AuditLogEntry = {
  id: string;
  caseId?: string;
  actorName: string;
  actorRole: string;         // "Admin", "Compliance", "System"
  actionType: AuditActionType;
  timestamp: string;
  details?: string;
  oldValue?: string;
  newValue?: string;
};

type LockedFundsSummary = {
  totalLockedGoldGrams: number;
  totalLockedUsdEquivalent: number;
  totalReleasedGoldGrams: number;
  activeCasesCount: number;
};
````

Mock state objects:

* `tradeCases: TradeCase[]`
* `tradeDocuments: TradeDocument[]`
* `approvalSteps: ApprovalStep[]`
* `auditLogs: AuditLogEntry[]`
* `lockedFundsSummary: LockedFundsSummary`
* Global reference: `currentGoldPriceUsdPerGram: number` for display only.

────────────────────────
OVERVIEW (DEFAULT)
────────────────────────

Top KPIs:

* Active Trade Cases
* Total Locked Settlement (grams)
* Locked Value (USD)
* Cases Under Review
* High/Critical Risk Cases

Below KPIs:

* Chart (mock): Locked vs Released gold over last 6 months.
* Table: “Top Risk Cases”

  * Case ID
  * Importer → Exporter
  * Jurisdiction Risk
  * AML flags count
  * Status
  * Action: “Open Case”

────────────────────────
TRADE CASES TAB
────────────────────────

Filters (top filter bar):

* Status (multi-select chips)
* Role of party (Importer/Exporter country)
* Risk level
* Shipment method (Sea/Air/Land)
* Search (by Case ID / Contract / Party)

Main table columns:

* Case ID (TF-XXXX-XXXX)
* Importer (name, country)
* Exporter (name, country)
* Value (USD)
* Value (Gold g)
* Locked Gold (g) + Lock Status
* Case Status
* Jurisdiction Risk
* AML Flags count
* Last Updated
* Action: “View”

Click row → **Trade Case Detail View** (drawer or full page).

────────────────────────
TRADE CASE DETAIL VIEW
────────────────────────

Header:

* Case ID + Contract Number.
* Status badge + Lock Status badge.
* Importer & Exporter mini cards (name, country, KYC, risk).

Use internal tabs:

* Overview
* Documents
* Approvals
* Locked Funds
* Audit

──────── OVERVIEW TAB ────────

Left side:

* Importer details (TradeParty):

  * Name, country, KYC status, risk level.
* Exporter details:

  * Same fields.

Right side:

* Trade Summary:

  * Commodity
  * Value USD
  * Value Gold g
  * Payment Terms
  * Delivery Terms (FOB/CIF/EXW)
  * Shipment Method
  * Expected Delivery Date
* Status badges:

  * Case Status (e.g. “Funded – Docs Pending”, “Under Review”)
  * Lock Status (“Reserved – Trade Finance Settlement Balance” if Locked)

Admin actions (buttons):

* “Mark as Funded (Lock Settlement)” – for demo:

  * Set `lockStatus = "Locked"`, `status = "Funded – Docs Pending"`.
  * Add Audit entry "FundingLocked".
* “Move to Under Review” – sets `status = "Under Review"`.
* “Mark as Approved – Ready to Release” – only if documents + approvals OK.
* “Release Funds to Exporter” – only if status = "Approved – Ready to Release".

On “Release Funds to Exporter”:

* Confirm modal:

  * Show lockedGoldGrams and approximate USD value.
  * Text: “This will instruct FinaVault to transfer settlement balance from importer to exporter. Continue?”
* On confirm:

  * `status = "Released"`, `lockStatus = "Released"`.
  * Add audit log "FundsReleased".
  * Comment in code:

    ```ts
    // TODO: Backend will:
    // - Debit Importer’s reserved gold in FinaVault / FinaFinance wallet
    // - Credit Exporter’s FinaPay / Trade wallet
    // - Update vault ledger entries
    ```

──────── DOCUMENTS TAB ────────

Use `tradeDocuments` filtered by caseId.

Show grouped by document type:

* Invoice
* Bill of Lading
* Packing List
* Certificate of Origin
* Insurance Certificate
* Inspection Report
* Other

Columns:

* Document Type
* File Name
* Status
* Uploaded By
* Uploaded At
* Actions:

  * “View” (open modal with placeholder preview)
  * “Approve”
  * “Reject”

When admin changes status:

* Update `TradeDocument.status`.
* Optionally add `remarks`.
* Add Audit entry "DocumentReviewed".

Show progress summary at top:

* X / Y Required Documents Approved.
* “Docs status: Incomplete / Pending Review / All Approved”.

──────── APPROVALS TAB ────────

Multi-level approval workflow:

Use `approvalSteps` for that case.

Vertical stepper / timeline:

Each step:

* Name (e.g. "Importer Verification", "Compliance Review", "Final Approval").
* Assigned Role (Ops / Compliance).
* Status (Pending / In Review / Approved / Rejected).
* Approver name + Date if completed.
* Notes.

Admin can:

* Click a step and change status:

  * "Mark In Review"
  * "Approve"
  * "Reject" (requires notes)
* When all mandatory steps Approved:

  * Automatically prompt: “Move case to: Approved – Ready to Release?”
  * On confirm:

    * `case.status = "Approved – Ready to Release"`
    * Add audit "StatusChanged".

──────── LOCKED FUNDS TAB ────────

Show settlement lock detail:

Card:

* “Locked Settlement Balance”
* Locked Gold: `lockedGoldGrams` g
* Approx USD value: `lockedGoldGrams * currentGoldPriceUsdPerGram`
* Lock Status: Not Locked / Locked / Released
* Text: “Locked funds cannot be sent, sold, staked, or used elsewhere until release.”

Timeline-style view:

* FinaPay → FinaFinance (lock request)
* FinaVault flag: “Reserved – Trade Finance Settlement Balance”
* Release (Importer → Exporter)

Include a small log list:

* When lock created
* When status changed
* When released

──────── AUDIT TAB ────────

Case-specific audit log:

Columns:

* Timestamp
* Actor (name + role)
* Action Type (CaseCreated, FundingLocked, DocumentReviewed, ApprovalUpdated, FundsReleased, StatusChanged)
* Details
* Old → New (short text)

Data: `auditLogs.filter(x => x.caseId === case.id)`.

────────────────────────
LOCKED FUNDS TAB (GLOBAL)
────────────────────────

Separate global view in main tabs:

Use `lockedFundsSummary` plus aggregated by case.

Top cards:

* Total Locked Gold (g)
* Locked USD Equivalent
* Released Gold (g)
* Active Locked Cases

Table:

* Case ID
* Importer → Exporter
* Locked Gold (g)
* Status
* Age of Lock (days)
* Action: “Open Case”

────────────────────────
COMPLIANCE & RISK TAB
────────────────────────

List of cases with compliance visibility:

Filters:

* Jurisdiction risk (Low/Medium/High/Critical)
* AML flags (any / yes / none)
* KYC status for importer/exporter

Table:

* Case ID
* Importer / Exporter
* Country
* Jurisdiction Risk
* AML Flags summary (chips)
* Value (USD)
* Status
* Action: Open case

Optional editable fields per case:

* Adjust risk level.
* Add “Compliance Notes” for that case.

Any change:

* Update case.riskLevel / notes.
* Add audit log "CaseUpdated" / "RiskUpdated".

────────────────────────
AUDIT LOG TAB (GLOBAL)
────────────────────────

Global timeline of all admin actions.

Filters:

* Date range
* Action type
* Actor
* Case ID

Table columns:

* Timestamp
* Case ID
* Actor (name + role)
* Action Type
* Details

────────────────────────
UX & ANIMATIONS
────────────────────────

* Use framer-motion (optional) for:

  * Drawer slide-in for case detail.
  * Fade-in for cards and tabs.
* Tailwind transitions on buttons and cards:

  * `transition-all duration-200 hover:border-[#D1A954] hover:shadow-lg hover:shadow-[#d1a95433]`
* Responsive:

  * On small screens, convert tables into stacked cards.
  * Filters collapse into a panel on mobile.

────────────────────────
GOAL
────────────────────────

Deliver a **FinaFinance / FinaBridge Admin Panel** that:

* Reflects the spec:

  * Trade case creation and lifecycle.
  * Locked gold settlement balance from FinaPay → FinaFinance → FinaVault.
  * Document management with multi-level approvals.
  * Secure release of funds after documents and approvals.
  * Full compliance/audit visibility.

* Uses only **front-end state and mock data**, but with clear structure and comments for future backend integration with:

  * FinaPay (wallet)
  * FinaVault (ledger)
  * Document store
  * Compliance APIs.

```

---

If you want next, I can give you a **small “Trade Case Detail only” Replit prompt** so your devs can isolate just that screen for faster iterations.
```
