You are a senior UI engineer and design-system specialist.

Goal:
Identify the EXACT existing UI color/template of the USER DASHBOARD (especially sidebar + topbar + cards + buttons)
and apply the SAME design consistently across EVERY component, modal, and form in the app.

IMPORTANT:
- Do NOT invent a new theme.
- Do NOT change the current brand look.
- First IDENTIFY what is already used in the user dashboard, then REUSE it everywhere.
- Remove inconsistent colors, shadows, borders, and spacing from other pages.

STEP 1 — IDENTIFY THE CURRENT USER DASHBOARD THEME (source of truth)
1) Open these pages in the app:
- /dashboard (user dashboard home)
- any page where the sidebar is visible
2) Inspect the DOM + CSS and extract:
- Background colors (app background, content area, sidebar, topbar)
- Text colors (primary, secondary, muted, placeholders)
- Border colors
- Card styles (radius, shadow, padding)
- Button styles (primary, secondary, danger, outline)
- Form styles (inputs, labels, helper text, validation errors)
- Modal styles (overlay opacity, modal background, radius, shadow)
- Spacing scale (gaps, padding)
- Font family + sizes
3) Find if theme is defined in:
- Tailwind config (tailwind.config.js)
- CSS variables (:root / globals.css)
- Theme provider (ThemeContext, shadcn theme, etc.)
Document the exact tokens you discover (hex values or CSS vars).

STEP 2 — CREATE/CONFIRM A SINGLE DESIGN TOKEN SYSTEM (NO NEW LOOK)
If tokens already exist, use them.
If inconsistent or scattered, consolidate into ONE place without changing values:
Option A) Tailwind tokens:
- Add/confirm colors in tailwind.config.js under theme.extend.colors
Option B) CSS variables:
- Define/confirm vars in globals.css, e.g.:
  --bg, --surface, --surface-2, --border, --text, --muted, --primary, --primary-foreground, --danger, --warning, --success

DO NOT change the extracted colors—only standardize how they’re referenced.

STEP 3 — BUILD REUSABLE UI PRIMITIVES
Create consistent components used everywhere:
- <AppShell> (layout wrapper + sidebar/topbar)
- <Card>, <CardHeader>, <CardBody>
- <Button> variants: primary/secondary/outline/ghost/danger
- <Input>, <Select>, <Textarea>
- <FormRow>, <FormLabel>, <FormHelp>, <FormError>
- <Modal> (overlay + container + header/footer)
- <Badge>, <Toast> styles

All pages/modals must use these primitives. No inline random styles.

STEP 4 — APPLY THE DASHBOARD THEME EVERYWHERE (refactor)
Search the codebase and replace inconsistent styles:
- hardcoded hex colors
- random Tailwind colors not in the dashboard theme
- inconsistent border radius / shadows
- mismatched modal overlays
Commands:
- Search for: "bg-" "text-" "border-" "shadow-" "rounded-" "modal" "dialog"
- Replace with token-based classes or shared components.

STEP 5 — SIDEBAR CONSISTENCY (critical)
Make sidebar identical on all pages:
- same width, same background, same hover/active state
- active route highlight consistent
- icons style consistent
- collapsed behavior consistent
Create SidebarNav config list once and reuse.

STEP 6 — FORMS + MODALS CONSISTENCY (critical)
All forms must look same:
- label color, required star, input height, border, focus ring
- error state (red border + message)
All modals must look same:
- overlay opacity
- modal radius + shadow
- header/footer layout
- close button style
Also fix z-index stacking issues.

STEP 7 — QUALITY CHECKLIST (must do)
- Run through all routes and confirm no “off-theme” screens:
  /dashboard
  /transactions
  /wallet
  /certificates
  /kyc
  /admin/* (admin should still match base theme unless separate admin theme exists)
- Verify dark/light mode behavior if present.
- Ensure contrast accessibility for text on sidebar and buttons.

OUTPUT
- Provide a “Theme Report”:
  - extracted tokens (colors + fonts + radius + shadows)
  - where tokens live now
  - what files changed
- Provide before/after screenshots (if E2E exists) or list of pages verified.

Start by extracting the exact colors from the current user dashboard sidebar/topbar, then implement the token consolidation, then refactor components/modals/forms to use the shared primitives.
