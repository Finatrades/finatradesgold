Here you go – **one final, merged Replit prompt** for **FinaBridge (Trade Finance + Wallet)** including:

* FinaBridge Wallet (Importer & Exporter)
* FinaPay → FinaBridge transfer
* Trade case creation
* Locking & releasing gold
* Documents, approvals, audit

Just **copy–paste this into Replit AI** 👇

---

````txt
You are a senior frontend engineer.
Build a complete **FinaBridge – Trade Finance Wallet** module UI for the Finatrades platform.

Concept:
FinaBridge (FinaFinance) is a GOLD-BASED TRADE FINANCE + WALLET module for:
- Importers
- Exporters
- Trade partners
- Finatrades Compliance (view in same UI, no separate login)

It must support:
- FinaBridge Wallet (Importer & Exporter)
- Transfer from FinaPay → FinaBridge Wallet (Importer side)
- Locking gold for trade cases (collateral/settlement)
- Trade Case creation and management
- Document Management System (DMS)
- Multi-level approval workflow
- Release of funds from Importer to Exporter
- Audit & Compliance view

No backend yet → use local state + mock data.

Tech:
- React (Vite + React or Next.js)
- TypeScript (preferred)
- TailwindCSS
- Optional: framer-motion for subtle animations

Theme / Branding:
- Dark premium gold-backed fintech
- Background gradient: #000000 → #0D0515 → #1A0A2E
- Accent gold: #D1A954
- Text: white and neutral grays (#E5E7EB, #9CA3AF)
- Cards: glassmorphism (bg-white/5, border-white/10, backdrop-blur, rounded-2xl)
- Font: "Inter" for UI (optionally "Playfair Display" for big headings)

────────────────────────
HIGH-LEVEL UI STRUCTURE
────────────────────────

Main route/component: `/finabridge` or `<FinaBridgeTrade />`

Layout:
- Top bar (title + quick info)
- FinaBridge Wallet strip (Importer/Exporter balances)
- Tabs:
  1. Trade Cases
  2. Create Trade Case
  3. Audit & Compliance

All inside a container: `max-w-6xl mx-auto px-4 py-6`.

────────────────────────
TOP BAR
────────────────────────

Top bar content:
- Left:
  - Title: "FinaBridge Trade"
  - Subtitle: "Gold-backed trade finance wallet for importers and exporters."
- Right:
  - Role badge: mock either "Importer" or "Exporter"
  - Small label: "Gold Spot: 2,350.40 USD/oz"
  - Optional icon for help.

────────────────────────
FINABRIDGE WALLET STRIP
────────────────────────

Directly below top bar, show a **FinaBridge Wallet** row.

This represents a separate wallet layer that sits between FinaPay and Trade Cases.

Two roles:

1) IMPORTER VIEW:
- Card title: "FinaBridge Wallet (Importer)"
- Show:
  - Available Balance: e.g. "250.00 g"
  - Locked for Trade Cases: e.g. "150.00 g"
  - Pending Release: e.g. "0.00 g"
- Secondary text: "Funds here are reserved for trade settlements; they are separate from your FinaPay wallet."
- Button: "Transfer from FinaPay"
  - On click, open modal:
    - Show:
      - "Available in FinaPay: 500.00 g" (mock)
      - Input: "Amount to transfer (g)"
    - On confirm:
      - Decrease FinaPay available balance
      - Increase `finaBridgeWallet.importer.availableGoldGrams`
      - Add audit log entry:
        "Transferred X.XX g from FinaPay to FinaBridge Wallet (Importer)."

2) EXPORTER VIEW:
- Card title: "FinaBridge Wallet (Exporter)"
- Show:
  - Available Balance: e.g. "40.00 g"
  - Incoming Locked Funds: e.g. "150.00 g"
    - Label: "Locked by Importers (Not Yet Released)"
- Secondary text:
  - "Locked funds represent gold reserved by importers for your trade cases. They become available after release."
- No transfer-in from FinaPay here; this wallet is fed by trade releases.

Implementation:
- Use a single component `<FinaBridgeWalletCard role="Importer" | "Exporter" ... />`
- But for now assume one role at a time (e.g., we can toggle role with a mock state).

────────────────────────
STATE: FINABRIDGE WALLET + FINAPAY
────────────────────────

Use TypeScript types:

```ts
type FinaBridgeWalletSide = {
  availableGoldGrams: number;
  lockedGoldGrams: number;
  incomingLockedGoldGrams?: number; // mainly for exporter
};

type FinaBridgeWallet = {
  importer: FinaBridgeWalletSide;
  exporter: FinaBridgeWalletSide;
};

const [finaPayBalanceGold, setFinaPayBalanceGold] = useState<number>(500); // mock
const [finaBridgeWallet, setFinaBridgeWallet] = useState<FinaBridgeWallet>({
  importer: {
    availableGoldGrams: 0,
    lockedGoldGrams: 0
  },
  exporter: {
    availableGoldGrams: 0,
    lockedGoldGrams: 0,
    incomingLockedGoldGrams: 0
  }
});
````

Transfers:

* FinaPay → FinaBridge (Importer):

  * Subtract from `finaPayBalanceGold`.
  * Add to `finaBridgeWallet.importer.availableGoldGrams`.

────────────────────────
TABS
────────────────────────

Create tabs below the wallet strip:

* Tab 1: "Trade Cases"
* Tab 2: "Create Trade Case"
* Tab 3: "Audit & Compliance"

Use a Tailwind tab design:

* Active tab: gold underline and bold text.
* Inactive: muted.

Switch content using local state.

────────────────────────
TAB 1: TRADE CASES LIST
────────────────────────

Purpose: list all existing trade cases.

Show a table:

Columns:

* Case ID (e.g., TF-2025-0007)
* Role (Importer/Exporter)
* Counterparty name
* Commodity
* Contract Value (USD + Gold equivalent)
* Locked Gold (g)
* Status:

  * "Draft"
  * "Awaiting Funding"
  * "Funded – Docs Pending"
  * "Under Review"
  * "Approved – Ready to Release"
  * "Released"
  * "Closed"
  * "Rejected"
* Action: "View"

Use colored status badges.

Button on top-right:

* "Create Trade Case" → switch to Create Trade Case tab.

Click "View" → open **Trade Case Detail** page/panel.

────────────────────────
TAB 2: CREATE TRADE CASE
────────────────────────

Purpose: allow corporate user (Importer/Exporter) to define a new gold-based trade.

Sections:

1. Basic Trade Details
2. Buyer & Seller Info
3. Shipment & Delivery Terms
4. Value & Settlement
5. Locking Settlement Value (from FinaBridge Wallet)
6. Summary & Actions

────────────────────────
SECTION 2.1: BASIC TRADE DETAILS

Fields:

* Role (radio): Importer / Exporter
* Case Name: text
* Trade Contract Number: text
* Purchase Order Number: text (optional)
* Expected Delivery Date (date)
* Commodity Description: textarea

────────────────────────
SECTION 2.2: BUYER & SELLER INFO

If user is Importer:

* Importer = current user
* Counterparty = Exporter info.

If user is Exporter:

* Exporter = current user
* Counterparty = Buyer info.

Fields (for counterparty block):

* Company Name
* Country
* Contact Person
* Contact Email

Fields:

* Payment Terms: dropdown or free text

  * "Advance Payment", "CAD", "Open Account", "LC", "Custom"
* Contract Reference (optional text)

────────────────────────
SECTION 2.3: SHIPMENT & DELIVERY TERMS

Fields:

* Delivery Terms (Incoterms): FOB / CIF / EXW / DAP / Other
* Shipment Method: Air / Sea / Land
* Shipment Route (text): e.g., "Dubai → Geneva"
* Port of Loading
* Port of Discharge
* Additional Notes: textarea

────────────────────────
SECTION 2.4: VALUE & SETTLEMENT

Fields:

* "Value Mode": radio

  * "Value in USD"
  * "Value in Gold (grams)"

If "USD":

* Total Contract Value (USD)
* Display computed equivalent gold:

  * `gold = usd / goldPriceUsdPerGram` (using a mocked price).

If "Gold":

* Total Contract Value (grams)
* Display equivalent USD.

Optionally:

* "Settlement % to Lock Now" (e.g. 100%, 50%).

Store in TradeCase object:

```ts
type TradeCaseStatus =
  | "Draft"
  | "Awaiting Funding"
  | "Funded – Docs Pending"
  | "Under Review"
  | "Approved – Ready to Release"
  | "Released"
  | "Closed"
  | "Rejected";

type TradeRole = "Importer" | "Exporter";

type TradeCase = {
  id: string;
  name: string;
  role: TradeRole;
  buyer: { company: string; country: string; contactName: string; email: string; };
  seller: { company: string; country: string; contactName: string; email: string; };
  commodityDescription: string;
  valueUsd: number;
  valueGoldGrams: number;
  paymentTerms: string;
  deliveryTerms: string;
  shipmentMethod: string;
  expectedDeliveryDate: string;
  lockedGoldGrams: number;
  status: TradeCaseStatus;
  createdAt: string;
  updatedAt: string;
};
```

────────────────────────
SECTION 2.5: LOCKING SETTLEMENT VALUE (FROM FINABRIDGE WALLET)

This is critical.

Flow:

* Importer must have already moved gold from FinaPay → FinaBridge Wallet (Importer).
* Lock for a trade comes **from** `finaBridgeWallet.importer.availableGoldGrams`.

UI:

* Show:

  * "Available in FinaBridge Wallet: X.XX g"
  * "Suggested Lock: Y.YY g" (based on contract value)
* Input:

  * "Gold to Lock (g)" (number, <= available).

Button:

* "Lock Settlement Value"

On click:

* Validate amount.
* Update state:

  * `importer.availableGoldGrams -= lockAmount`
  * `importer.lockedGoldGrams += lockAmount`
* Attach this lock to the current draft TradeCase:

  * `tradeCase.lockedGoldGrams = lockAmount`
* Set trade case status:

  * If lockAmount > 0: "Funded – Docs Pending"
  * Else: "Awaiting Funding"
* Add AuditLogEntry:

  * Actor: Importer
  * ActionType: "Lock Gold"
  * Details: "Locked X.XX g for Case TF-XXXX."

Conceptual comment:

```ts
// TODO: FinaVault would mark this amount as
// "Reserved – Trade Finance Settlement Balance" for this trade case.
```

────────────────────────
SECTION 2.6: SUMMARY & ACTIONS

Summary card shows:

* Case Name
* Counterparty
* Total Value (USD + Gold)
* Locked Gold (g)
* Role
* Shipment method, Incoterm, Payment terms.

Buttons:

* "Save as Draft" → create case with status "Draft"
* "Create Trade Case" → if lockedGold > 0 → "Funded – Docs Pending" else "Awaiting Funding"

On submit:

* Add new TradeCase to `tradeCases` state.
* Navigate to Trade Cases tab or directly to Trade Case Detail with toast "Trade case created."

────────────────────────
TRADE CASE DETAIL VIEW
────────────────────────

When user clicks "View" from Trade Cases list, show a full detail screen.

Layout:

* Header + summary (top)
* Inner tabs:

  * Overview
  * Documents
  * Approvals
  * Ledger & Audit

──────── OVERVIEW TAB ────────

Show:

* Case ID, Case Name, Status badge, Role
* Locked Gold (g)
* Contract Value (USD + Gold)
* Delivery Terms, Payment Terms, Shipment Method, Timeline
* Buyer/Seller info blocks

Importer-specific:

* If status = "Approved – Ready to Release":

  * Show prominent button:

    * "Release Settlement to Exporter"

On Release:

1. Decrease `finaBridgeWallet.importer.lockedGoldGrams` by case.lockedGoldGrams.
2. Decrease `finaBridgeWallet.exporter.incomingLockedGoldGrams` by same amount.
3. Increase `finaBridgeWallet.exporter.availableGoldGrams` by same amount.
4. Update case status → "Released".
5. Add AuditLogEntry:

   * Actor: Importer
   * Action: "Release Gold"
   * OldValue/NewValue: before and after wallet snapshots (stringified).

Concept comment for future:

```ts
// TODO: FinaVault transfers legal ownership from Importer → Exporter
// and credit Exporter FinaPay or FinaBridge wallet accordingly.
```

Exporter-specific:

* In Overview, show:

  * "Locked by Importer: X.XX g (Not yet released)" when case status is funded but not released.

──────── DOCUMENTS TAB (DMS) ────────

Implement a Document Management System per case.

Document types:

* Invoice
* Bill of Lading
* Packing List
* Certificate of Origin
* Insurance Certificate
* Inspection Report
* Other Contract Document

Features:

* Upload area:

  * Simulate file upload via input and store metadata in state.
* List documents:

  * Type
  * File name
  * Version (1, 2, 3…)
  * UploadedBy
  * UploadedAt
  * DigitalSignatureStatus: "Unsigned" | "Signed" | "Rejected" (optional)

Buttons:

* "Upload / Replace" → increments version for that document type.
* "View" → open preview modal with placeholder.
* "Sign Digitally" → change status to "Signed".

`TradeDocument` type:

```ts
type TradeDocument = {
  id: string;
  caseId: string;
  type: string;
  fileName: string;
  version: number;
  uploadedBy: string;
  uploadedAt: string;
  digitalSignatureStatus?: "Unsigned" | "Signed" | "Rejected";
};
```

──────── APPROVALS TAB ────────

Multi-level approval workflow:

Steps:

1. Exporter Uploads Documents
2. Importer Verifies
3. Finatrades Compliance Reviews
4. Final Approval
5. Ready for Release

Represent as vertical or horizontal stepper:

`ApprovalStepStatus`:

* "Pending"
* "In Review"
* "Approved"
* "Rejected"

`ApprovalStep`:

```ts
type ApprovalStepStatus = "Pending" | "In Review" | "Approved" | "Rejected";

type ApprovalStep = {
  id: string;
  caseId: string;
  name: string; // "Importer Verification", etc.
  role: string; // "Importer", "Exporter", "Compliance"
  status: ApprovalStepStatus;
  approverName?: string;
  decisionAt?: string;
  notes?: string;
};
```

UI:

* For each step:

  * Show title, role, status badge.
  * Buttons: "Approve", "Reject" (for simulation).
  * Textarea for notes.

Behavior:

* Approve:

  * status -> Approved
  * store approverName + decisionAt + notes
* Reject:

  * status -> Rejected
  * set Case status -> "Rejected"

When all required steps Approved:

* Case status -> "Approved – Ready to Release".

──────── LEDGER & AUDIT TAB ────────

Show full audit trail:

`AuditLogEntry`:

```ts
type AuditLogEntry = {
  id: string;
  caseId: string;
  actorName: string;
  actorRole: string;
  actionType: string; // "Create Case", "Lock Gold", "Release Gold", "Upload Document", "Approve", etc.
  timestamp: string;
  ipAddress?: string;
  deviceId?: string;
  oldValue?: string;
  newValue?: string;
  details?: string;
};
```

Render table:

* Time
* Actor (name + role)
* ActionType
* Details
* Old→New (optional short version)

Append audit log entries whenever:

* Case created
* Documents uploaded/replaced
* Steps approved/rejected
* Gold locked
* Funds released
* Changes are made to key fields.

────────────────────────
TAB 3: AUDIT & COMPLIANCE (GLOBAL)
────────────────────────

This is a mini compliance dashboard.

Show:

Summary cards:

* Cases with AML flags
* Cases under compliance review
* Cases rejected
* Cases approved this month

Table of cases:

* Case ID
* Role
* Counterparty
* Jurisdiction Risk: Low/Medium/High (mock)
* AML Status: "Clear" / "Flagged"
* Status
* Action: "Open Case"

Filters:

* Status filter dropdown
* Role filter
* Risk level filter

Use mock `complianceInfo` tied to each TradeCase.

────────────────────────
ANIMATIONS & UX
────────────────────────

* Use subtle framer-motion or Tailwind transitions:

  * Cards fade-in + slight upward motion on mount.
  * Buttons hover: scale 1.02, gold border glow.
  * Tabs slide content in.

* Fully responsive:

  * On mobile: tables collapse into stacked cards.
  * Tabs and wallet strip still readable.

────────────────────────
FINAL GOAL
────────────────────────

Deliver a polished, enterprise-grade **FinaBridge Trade Finance + Wallet** module that:

* Shows Importer & Exporter **FinaBridge Wallet** balances.
* Allows transferring gold from FinaPay → FinaBridge (Importer).
* Allows locking gold for a specific trade case.
* Lets Exporter see **locked but not yet released** funds.
* Supports full Trade Case creation.
* Provides DMS (documents per case).
* Implements multi-step approval workflow.
* Simulates fund release and case status updates.
* Includes audit logging and a compliance overview.

All data is front-end only (mocked) but structured so backend integration will be straightforward.

```

---

If you want, I can now give you a **unified `models.ts` Replit prompt** for all modules (FinaVault + FinaPay + FinaBridge + BNSL) so your devs have one shared type file.
```
