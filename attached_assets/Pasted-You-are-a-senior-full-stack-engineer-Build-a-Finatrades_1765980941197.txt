You are a senior full-stack engineer. Build a Finatrades “All Transactions” system in Replit.

GOAL
- Fetch and display ALL transaction types from all modules in ONE unified view.
- Transactions must auto-refresh (no manual reload).
- New transactions appear instantly after any button action.
- Must support filtering by module, type, date, status, and user.

STACK
- Next.js (App Router) + React + TypeScript
- Tailwind CSS
- TanStack Query (React Query)
- Backend: Next.js Route Handlers (/api/*)
- DB: Prisma + SQLite (fallback to in-memory store if needed)

MODULES TO COVER
- FinaPay
- FinaVault
- BNSL
- FinaBridge
- Admin / Control Tower

SINGLE SOURCE OF TRUTH (VERY IMPORTANT)
Use ONE unified table / model for all transactions.

Transaction Model (mandatory fields)
- id
- userId
- counterpartyUserId (nullable)
- module: FINAPAY | FINAVAULT | BNSL | FINABRIDGE
- actionType:
  ADD_FUNDS
  SEND
  RECEIVE
  LOCK
  UNLOCK
  CREATE_TRADE
  APPROVE_TRADE
  RELEASE_SETTLEMENT
- grams
- usd
- usdPerGram
- status: PENDING | LOCKED | COMPLETED | FAILED | REVERSED
- referenceId (tradeCaseId / planId / transferId)
- description
- createdAt

FETCH REQUIREMENTS
1) API ENDPOINT (main)
GET /api/transactions
Query params:
- module
- actionType
- status
- userId
- fromDate
- toDate
- limit (default 50)
- cursor (for pagination)

Response:
- transactions[]
- nextCursor
- totals:
  - totalGrams
  - totalUSD

2) MODULE-SPECIFIC ENDPOINTS (optional)
- GET /api/finapay/transactions
- GET /api/bnsl/transactions
- GET /api/finabridge/transactions
(All must internally read from the SAME transactions table)

FRONTEND UI
Page: /transactions (User)
- Tabs or filters:
  All | FinaPay | FinaVault | BNSL | FinaBridge
- Filters:
  Date range
  Status
  Action type
- Table columns:
  Date | Module | Action | Grams | USD | Status | Reference
- Clicking a row opens:
  - full transaction details
  - linked Trade Case / BNSL Plan (if any)

ADMIN VIEW
Page: /admin/transactions
- See all users’ transactions
- Filter by user, module, date
- Export CSV button (stub ok)

AUTO-REFRESH & SPEED RULES
- All transaction reads use React Query
- staleTime: 10–30s
- refetchOnWindowFocus: true
- After ANY mutation anywhere in the app:
  invalidateQueries(["transactions"])
  invalidateQueries(["balances"])
- Use optimistic updates:
  - new transaction row appears instantly
  - rollback if API fails
- Add “Live” indicator:
  🔄 Syncing | ✅ Updated | ⚠️ Error

SEED DATA
- Seed sample transactions for all modules and action types.
- Seed admin + normal user.

IMPORTANT UX RULES
- No full page reloads
- New transactions appear instantly
- Filters must not reset on refresh
- Table should handle 1000+ rows (pagination or infinite scroll)

DELIVERABLES
- Working unified transaction system in Replit
- Clean, readable code
- README explaining:
  - how transactions are recorded
  - how auto-refresh works
  - how to test (add funds, lock gold, release trade → see instantly)
