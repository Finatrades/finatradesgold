Act as a senior UI/UX architect, front-end engineer, and product owner.

PROJECT CONTEXT:
This is a production-grade fintech / eCommerce homepage & landing page.
Goal is Swiss-grade, premium, trust-first UI with perfect responsiveness.

TASK:
1. Perform a COMPLETE UI/UX audit of:
   - Homepage
   - Landing Page

2. Test across ALL viewports:
   - Desktop (1440px, 1366px, 1280px)
   - Laptop
   - Tablet (iPad portrait & landscape)
   - Mobile (iPhone SE, 12, 13, 14, Android small & large)
   - Extreme small screens (≤360px)

3. Validate RESPONSIVENESS:
   - No horizontal scroll
   - No clipped content
   - No overlapping sections
   - Proper container widths
   - Correct padding & margin scaling
   - Images scale correctly (object-fit, aspect ratio)
   - Fonts resize smoothly using clamp() or responsive units

4. NAVIGATION & INTERACTION CHECK:
   - Header & footer alignment
   - Mobile hamburger menu works flawlessly
   - CTA buttons clickable (no dead zones)
   - No click opens from wrong position
   - Scroll behavior smooth
   - Anchor links correct

5. TYPOGRAPHY AUDIT:
   - Heading hierarchy (H1 → H6)
   - Line-height readability
   - Font consistency
   - No text overflow on mobile
   - Button text always visible

6. VISUAL CONSISTENCY:
   - Color contrast (WCAG compliant)
   - Consistent spacing system (8px / 4px grid)
   - Card alignment
   - Shadows & borders uniform
   - No broken animations

7. PERFORMANCE UI CHECK:
   - Lazy-load images
   - No layout shift (CLS)
   - Smooth animations only
   - Avoid heavy JS for layout

8. ACCESSIBILITY (BASIC):
   - Click targets ≥44px
   - Readable text
   - Focus states visible
   - Proper ARIA labels if applicable

9. FIX & IMPLEMENT:
   - Automatically fix all detected UI issues
   - Refactor CSS if needed
   - Use modern responsive techniques:
     • Flexbox / Grid
     • clamp()
     • max-width containers
     • media queries only where needed

10. OUTPUT:
   - Summary of issues found
   - List of fixes applied
   - Updated components & CSS
   - Final confirmation: "Desktop & Mobile UI is production-ready"

IMPORTANT:
Think like a real end-user AND a fintech product owner.
No assumptions. Test everything.
Do not skip edge cases.
