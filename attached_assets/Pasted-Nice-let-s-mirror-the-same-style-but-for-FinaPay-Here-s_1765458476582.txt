Nice, let’s mirror the same style but for **FinaPay**.
Here’s your **ready-to-paste Replit dev prompt** 👇

---

````txt
You are a senior frontend engineer.
Build a **FinaPay Wallet Dashboard** UI for the Finatrades platform.

Context:
FinaPay is the **primary user-facing digital gold wallet** in the Finatrades ecosystem.

- FinaVault is the source of truth for physical gold ownership.
- FinaPay is the UI layer that:
  - Reads gold balances from FinaVault.
  - Triggers buy/sell/send/request flows.
  - Shows valuations in USD / AED.
  - Connects to:
    - BNSL (Buy Now Sell Later plans)
    - FinaBridge / FinaFinance (Trade Finance wallet)
    - FinaCard (debit card wallet)
- FinaPay itself does NOT hold gold; it only reflects FinaVault ledger state via API.

For this task: **frontend only**, use **mock data + local state**.

Tech:
- React (Vite + React or Next.js)
- TypeScript
- TailwindCSS
- Optional: framer-motion for subtle animations

Theme:
- Dark, premium, gold-fintech:
  - Background gradient: #000000 → #0D0515 → #1A0A2E
  - Accent gold: #D1A954
  - Cards: glassy (bg-white/5, border-white/10, backdrop-blur, rounded-2xl, shadow)
- Font: Inter

────────────────────────
ROUTE & LAYOUT
────────────────────────

Create component `/finapay` or `<FinaPayDashboard />`.

Layout:
- Left sidebar: navigation.
- Right content:
  - Header (wallet title + user info)
  - KPI cards
  - Live gold chart
  - Quick actions
  - Transactions table

Sidebar menu:
- FinaPay Wallet (active)
- FinaVault (link stub)
- BNSL Plans (stub)
- FinaBridge (Trade Finance) (stub)
- FinaCard (stub)
- Settings / Limits

Header:
- Title: “FinaPay – Digital Gold Wallet”
- Subtitle: “View your gold balance, valuations and move value across Finatrades.”

────────────────────────
TYPES & MOCK STATE
────────────────────────

Use TypeScript types.

```ts
type Currency = "USD" | "AED";

type WalletTier = "Standard" | "Gold" | "Platinum";

type TransactionType =
  | "BuyGold"
  | "SellGold"
  | "SendGold"
  | "ReceiveGold"
  | "RequestGold"
  | "BNSLJoin"
  | "BNSLReward"
  | "TradeLock"
  | "CardTopUp"
  | "Fee";

type TransactionStatus = "Pending" | "Completed" | "Failed" | "Reversed";

type CounterpartyType = "InternalUser" | "ExternalWallet" | "System";

type FundingMethod = "BankTransfer" | "Card" | "Crypto";
type PayoutMethod = "BankTransfer" | "CardRefund" | "CryptoPayout";

type FinaPayWallet = {
  userId: string;
  walletId: string;
  tier: WalletTier;
  goldBalanceGrams: number;     // live from FinaVault (mock)
  goldValueUsd: number;
  goldValueAed: number;
  availableGoldGrams: number;   // free (not locked in BNSL / Trade)
  lockedForBnslGrams: number;
  lockedForTradeGrams: number;
  pendingSettlementGrams: number;
  lastUpdated: string;
};

type GoldPricePoint = {
  timestamp: string;
  priceUsd: number;
};

type FinaPayTransaction = {
  id: string;
  type: TransactionType;
  status: TransactionStatus;
  createdAt: string;

  goldGrams?: number;
  amountUsd?: number;
  amountAed?: number;

  fromWalletId?: string;
  toWalletId?: string;
  counterpartyLabel?: string;
  counterpartyType?: CounterpartyType;

  reference?: string;          // external ref / case ID
  note?: string;
  feeUsd?: number;
};

type QuickAction =
  | "BuyGold"
  | "SellGold"
  | "SendGold"
  | "RequestGold"
  | "MoveToBnsl"
  | "MoveToTrade"
  | "TopUpCard";

type BuyGoldDraft = {
  fundingMethod: FundingMethod;
  inputMode: "USD" | "Grams";
  amountUsd?: number;
  amountGrams?: number;
  estimatedPriceUsdPerGram: number;
};

type SellGoldDraft = {
  payoutMethod: PayoutMethod;
  goldGrams: number;
  estimatedAmountUsd: number;
};

type SendGoldDraft = {
  recipientIdentifier: string;   // email / wallet ID
  goldGrams: number;
  memo?: string;
};

type RequestGoldDraft = {
  requesterNote?: string;
  recipientIdentifier: string;
  goldGrams: number;
};

type LimitsConfig = {
  dailyBuyUsd: number;
  dailySellUsd: number;
  dailySendGoldGrams: number;
  remainingBuyUsdToday: number;
  remainingSellUsdToday: number;
  remainingSendGoldGramsToday: number;
};

type AmlFlag = {
  id: string;
  level: "Info" | "Review" | "Block";
  reason: string;
  createdAt: string;
};

type FinaPayContext = {
  wallet: FinaPayWallet;
  goldPriceHistory: GoldPricePoint[];
  transactions: FinaPayTransaction[];
  limits: LimitsConfig;
  amlFlags: AmlFlag[];
  currentGoldPriceUsdPerGram: number;
};
````

Mock state:

* `finapayContext: FinaPayContext`
* plus local draft states for each modal (buy/sell/send/request).

────────────────────────
HOME DASHBOARD
────────────────────────

Top KPI cards (row of 4–5 cards):

1. **Gold Balance**

   * `wallet.goldBalanceGrams` g
   * Subtext: “Total gold holdings reflected from FinaVault.”

2. **Gold Value (USD)**

   * `$wallet.goldValueUsd`
   * Subtext: “Estimated valuation at live LBMA-linked rate.”

3. **Gold Value (AED)**

   * `wallet.goldValueAed` (assuming static FX in mock)

4. **Available vs Locked**

   * Available: `availableGoldGrams` g
   * Locked: `(lockedForBnslGrams + lockedForTradeGrams)` g
   * Use a small bar or donut visual.

5. **Tier & Limits**

   * Tier: Standard / Gold / Platinum
   * Short line: “Daily send limit: XX g”

Below KPI row:

Section 1 – **Live Gold Spot Chart**

* Small area chart, using `goldPriceHistory`:

  * Toggle: 24h / 7d / 30d (change mock data subset).
* Overlay: show current price `currentGoldPriceUsdPerGram`.

Section 2 – **Quick Actions**
Grid of action buttons with icons:

* Buy Gold
* Sell Gold
* Send Gold
* Request Gold
* Move to BNSL
* Move to Trade Finance (FinaBridge)
* Top Up FinaCard

Clicking opens modals (see flows below).

Section 3 – **Recent Transactions**
Table with last 8–10 transactions.

Columns:

* Date & time
* Type (Buy / Sell / Send / Receive / BNSL Join / Trade Lock / CardTopUp)
* Direction:

  * e.g. “You → John Doe (SendGold)”
  * “System → You (BNSL Reward)”
* Gold (g)
* Amount (USD / AED if relevant)
* Status badge
* Reference

Add pill filters above table:

* All | Buy | Sell | Transfers | BNSL | Trade | Card

────────────────────────
BUY GOLD FLOW (MODAL)
────────────────────────

Trigger: Quick Action “Buy Gold”.

Modal layout:

* Title: “Buy Gold”
* Subtitle: “Convert fiat or crypto to vaulted gold.”

Fields:

* Funding Method (segmented control):

  * Bank Transfer
  * Card (Visa/Mastercard)
  * Crypto
* Input Mode toggle:

  * “Enter in USD”
  * “Enter in Grams”
* If USD:

  * Field: `Amount (USD)`
  * Read-only: `Estimated grams = amountUsd / currentGoldPriceUsdPerGram`
* If Grams:

  * Field: `Gold (grams)`
  * Read-only: `Estimated USD = grams * currentGoldPriceUsdPerGram`

Summary card:

* Current Price: `$X,XXX.XX / g`
* Est. Gold: XX.XXX g
* Fee (mock): e.g. 0.25%
* Total (USD)

CTA:

* Primary: “Confirm & Buy”
* Secondary: “Cancel”

On confirm (mock):

* Add `FinaPayTransaction` with type "BuyGold", status "Completed".
* Update wallet:

  * Increase `goldBalanceGrams`, `availableGoldGrams`.
* Add comment in code:

```ts
// TODO: Backend will:
// - Process payment via selected funding method
// - Credit gold grams in FinaVault
// - Sync updated balance to FinaPay
```

────────────────────────
SELL GOLD FLOW (MODAL)
────────────────────────

Trigger: “Sell Gold”.

Fields:

* Payout Method:

  * Bank Transfer
  * Crypto payout
  * Card refund
* Gold to sell (grams)

  * Show available balance.
* Estimated payout:

  * `grams * currentGoldPriceUsdPerGram` (minus mock fee)
* Limits info:

  * Show if exceeds `limits.remainingSellUsdToday` (mark error).

CTA:

* “Confirm & Sell”
* “Cancel”

On confirm:

* Decrease `wallet.goldBalanceGrams` + `availableGoldGrams`.
* Create transaction `type = "SellGold"`.

With comment:

```ts
// TODO: Backend:
// - Debit grams from FinaVault
// - Trigger payout via selected method
// - Update wallet + FinaVault ledger
```

────────────────────────
SEND GOLD FLOW (MODAL)
────────────────────────

Trigger: “Send Gold”.

Fields:

* Recipient identifier:

  * Input: email / wallet ID.
  * (In mock: just a string + validation “min 3 chars”).
* Gold amount (grams)
* Optional memo.

Validation:

* Must be ≤ availableGoldGrams.
* Show remaining daily send limit from `limits.remainingSendGoldGramsToday`.

Summary text:

* “You are sending XX.XXX g (≈ $YYY.YY) to [recipient]. This is an internal transfer on Finatrades.”

CTA:

* “Send Gold”
* “Cancel”

On confirm:

* Add transaction:

  * Sender: this wallet, type = "SendGold"
  * A corresponding "ReceiveGold" transaction can be simulated (mock).
* Reduce `availableGoldGrams`.

Note (in UI helper text):

* “All transfers are instant and fully reflected in FinaVault ownership records.”

────────────────────────
REQUEST GOLD FLOW (MODAL)
────────────────────────

Trigger: “Request Gold”.

Fields:

* Recipient identifier (email / wallet ID).
* Gold amount (grams).
* Request note.

After submit:

* Show confirmation banner in UI: “Request sent to [recipient].”
* Add transaction:

  * type = "RequestGold"
  * status = "Pending"

Helper text:

* “If the recipient approves, a Send Gold operation will execute automatically.”

────────────────────────
MOVE TO BNSL (LOCK GOLD) – FLOW (STUB)
────────────────────────

Trigger: “Move to BNSL”.

Modal or side card:

* Title: “Move Gold to BNSL Plan”
* Fields:

  * Gold amount (grams)
  * Plan tenor: 12 / 24 / 36 months (just a select).
* Info:

  * Note that these grams will be marked as “locked for BNSL” and will not be available in FinaPay.

On confirm:

* Decrease `availableGoldGrams`.
* Increase `lockedForBnslGrams`.
* Create transaction type "BNSLJoin".

Add comment:

```ts
// TODO: Backend:
// - Lock grams in FinaVault under BNSL module
// - Create BNSL plan record
// - Reflect lock in wallet
```

────────────────────────
MOVE TO TRADE FINANCE (FinaBridge) – FLOW (STUB)
────────────────────────

Trigger: “Move to Trade Finance”.

Modal:

* Title: “Transfer Gold to FinaBridge”
* Fields:

  * Gold amount (grams)
  * Optional: Link to Trade Case (input ID or dropdown of mock cases).
* On confirm:

  * Decrease `availableGoldGrams`.
  * Increase `lockedForTradeGrams`.
  * Create transaction type "TradeLock".

Comment:

```ts
// TODO: Backend:
// - Transfer grams from FinaPay to FinaFinance trade wallet
// - Mark in FinaVault as "Reserved – Trade Finance Settlement Balance"
```

────────────────────────
TOP UP FINA CARD (STUB)
────────────────────────

Trigger: “Top Up Card”.

Modal:

* Fields:

  * Gold to convert (grams)
* Show:

  * Estimated fiat equivalent (USD/AED).
  * Note: “Converted value will appear as spending balance on your FinaCard. Gold will be debited from FinaVault.”

On confirm:

* Reduce `availableGoldGrams`.
* Create transaction type "CardTopUp".

Comment for devs:

```ts
// TODO:
// - Debit grams in FinaVault
// - Credit card wallet in fiat
// - Sync balances
```

────────────────────────
LIMITS & AML SECTION
────────────────────────

Right-side panel or separate “Settings / Limits” section:

Show:

* Daily / monthly limits from `limits`.
* Remaining headroom today.
* AML flags (if any):

  * List `amlFlags` with severity.

Visual:

* If there is any `level = "Block"`:

  * Show red banner: “Some actions may be blocked pending compliance review.”

This is display-only for now.

────────────────────────
UX & ANIMATIONS
────────────────────────

* Use framer-motion (optional) for:

  * Modals fading in / scaling.
  * KPI cards subtle entrance on load.
* Tailwind transitions:

  * `transition-all duration-200 hover:border-[#D1A954] hover:shadow-lg hover:shadow-[#d1a95433]`
* Responsive:

  * On mobile, stack KPI cards vertically.
  * Turn transactions table into list cards.

────────────────────────
GOAL
────────────────────────

Deliver a **FinaPay Wallet Dashboard** that:

* Reflects the real business logic:

  * Gold is stored in FinaVault.
  * FinaPay is just the operational wallet UI.
  * Users can buy, sell, send, request, and route gold into BNSL, Trade, and Card.
* Is fully front-end with mock state:

  * All actions update local state and create `FinaPayTransaction` entries.
  * All backend calls are clearly marked with `// TODO` comments for engineers.
* Has clean, premium gold-fintech UI, suitable to sit next to FinaVault, FinaBridge, BNSL, and FinaCard modules in the same design system.

```

---

If you’d like, I can also give you a **smaller Replit prompt just for the FinaPay “Buy/Sell/Send” modal components**, so your devs can isolate and reuse them across web + mobile.
```
