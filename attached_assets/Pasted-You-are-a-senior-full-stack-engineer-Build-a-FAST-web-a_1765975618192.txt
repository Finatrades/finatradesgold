You are a senior full-stack engineer. Build a FAST web app in Replit with:

GOAL
When user clicks any action button, the UI must auto-update instantly (no manual refresh).
Auth must be solid: sign-in, sign-out, session expiry, auto refresh token, and show login/logout time.
System must feel “snappy”: optimistic UI, caching, minimal re-renders, fast API calls.

TECH STACK (choose the fastest simple setup)
- Frontend: Next.js (App Router) + React + TypeScript
- Styling: Tailwind
- Data fetching: TanStack Query (React Query)
- Auth: NextAuth (Credentials or OAuth placeholder) OR custom JWT + refresh token
- Backend: Next.js Route Handlers (/api/*)
- DB: SQLite (Prisma) OR in-memory stub if DB not needed yet

CORE REQUIREMENTS
1) AUTH FLOW
- Sign In page: email + password (stub ok)
- On successful sign-in:
  - store session securely (httpOnly cookies if custom JWT, or NextAuth session)
  - record login timestamp in DB (AuthAudit table)
  - show “Signed in at: {time}” in header
- Sign Out:
  - clear session
  - record logout timestamp
  - show “Last logout: {time}”
- Auto session refresh:
  - if access token expires, refresh silently using refresh token endpoint
  - if refresh fails → force sign-out and redirect to /login
- Idle timeout:
  - if no activity for X minutes, auto sign out (configurable)

2) AUTO UPDATE ON BUTTON CLICK (NO MANUAL REFRESH)
- Use React Query everywhere.
- For every mutation (e.g., Add Funds / Transfer / Create Request):
  - immediately update UI (optimistic update)
  - call API
  - on success: invalidate queries + refetch in background
  - on error: rollback optimistic update + show toast error

3) FAST REFRESH / REALTIME FEEL
- Use:
  - refetchOnWindowFocus = true
  - staleTime = 10-30s
  - polling only for critical counters (optional, 5-10s)
- Add a “Sync status” indicator:
  - ✅ Up to date / 🔄 Syncing / ⚠️ Offline
- Add global loading bar + toast notifications.

4) PAGES / UI (simple but complete)
- /login
- /dashboard
  - Header: user email, Signed-in time, Logout button
  - Widgets: (stub data ok)
    - Balance cards
    - Recent activity table
    - Buttons: “Refresh Now”, “Test Update Button”
- Clicking “Test Update Button” should:
  - create a new activity row via POST /api/activity
  - UI updates instantly with the new row (optimistic)
  - then confirms with server and refetches.

5) API ENDPOINTS (MUST IMPLEMENT)
- POST /api/auth/login
- POST /api/auth/logout
- POST /api/auth/refresh
- GET  /api/me
- GET  /api/activity
- POST /api/activity

6) DATA MODELS (Prisma schema or simple JSON store)
- User: id, email, passwordHash
- Session: id, userId, accessToken, refreshToken, expiresAt, createdAt
- AuthAudit: id, userId, loginAt, logoutAt, ip, userAgent
- Activity: id, userId, type, message, createdAt

7) PERFORMANCE RULES
- Avoid unnecessary re-renders (memoize heavy components)
- Use server-side pagination for activity if > 50
- Keep API responses small
- Use proper error boundaries
- Provide clean folder structure and readable code comments.

DELIVERABLES
- Working app running in Replit
- Seed user: admin@finatrades.test / Password123!
- Clear instructions in README: run, login, test “auto update” behavior
- All auth logic must be safe-by-default (httpOnly cookies if custom JWT)

IMPORTANT UX
- No full page reloads for actions.
- After any button click, user sees immediate result.
- Session refresh happens silently in background.
